all:
  hosts:
      inetrouter:
           ansible_port: 2001
           ansible_ssh_private_key: '~/.ssh/id_rsa' 

  children:
    routers:
      hosts:
        centralrouter:
           ansible_port: 2020
           ansible_ssh_private_key: '~/.ssh/id_rsa' 
    servers:
      hosts:
        centalserver:
           ansible_port: 2021
           ansible_ssh_private_key: '~/.ssh/id_rsa' 
  vars:
    ansible_user: vagrant
    ansible_host: 127.0.0.1
    test_var: 'hahaha'

